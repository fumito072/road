# 2026-04-06 進捗ログ

## 実施内容

### Phase 1: 基盤構築
- モノレポ構成を構築（frontend/ と backend/ の分離、npm workspaces）
- NestJS バックエンドをスキャフォールド（app.module, main.ts, nest-cli.json 等）
- Prisma スキーマを作成（User, Tab, Upload, UploadFile + UploadStatus enum）
- Firebase Admin SDK による認証ガード（FirebaseAuthGuard）を実装
- 各モジュールを作成（Auth, Tabs, Uploads, OCR, SharePoint）
- Controller / Service / DTO / Module パターンで統一

### Phase 1.5: 環境構築
- PostgreSQL（Homebrew、既にインストール済み）の起動確認
- `road_dev` データベースを作成
- `backend/.env` を作成（DATABASE_URL, FIREBASE_PROJECT_ID, NODE_ENV 等）
- `npx prisma migrate dev --name init` でテーブル作成（users, tabs, uploads, upload_files）
- NestJS 起動確認（ポート 3001）
- デフォルト 5 タブのシードスクリプト作成・実行

### Phase 2: タブシステム構築
- 開発モード認証バイパスを追加（NODE_ENV=development で Firebase トークン不要）
- フロントエンド API クライアント作成（frontend/lib/api.ts）
- Tab 型定義作成（frontend/lib/types.ts）
- タブバーコンポーネント作成（API からタブ取得、アイコン表示、アクティブ状態）
- タブ設定モーダル作成（名前・アイコン・OCRプロンプト・SharePoint設定の CRUD）
- ページ統合（タブ選択 → OCR Workbench 表示の流れ）
- NestJS の AuthModule 依存関係エラーを修正（TabsModule, UploadsModule に AuthModule import 追加）

### 運用ルール整備
- `.company/CLAUDE.md` に AI 運用ルールを記載
- `.company/note/` と `.company/todo/` の運用フォーマットを策定

## 決定事項
- PostgreSQL + Prisma 構成に確定（Firestore から変更）
- Firebase Authentication はフロントエンド直接、NestJS は ID トークン検証のみ
- 開発中は NODE_ENV=development で認証スキップ
- `.company/` ディレクトリで AI の進捗管理・TODO 管理を行う運用に変更

## 課題・メモ
- OCR サービス（ocr.service.ts）は TODO のプレースホルダー状態。Gemini API キーが必要
- SharePoint サービス（sharepoint.service.ts）も同様。Graph API 認証情報が必要
- フロントエンドのタブバーはブラウザで動作確認済みだが、スクリーンショットではタブが表示されていない可能性あり（要確認）

### Phase 2.5: タブ名変更 + UI 統合
- デフォルトタブ名を実業務カテゴリに変更: コラボ, モバイル, リース・現金, 電力, 酒井（領収書）
- seed.ts を更新し、DB を再シード（DELETE + seed 実行）
- アイコンを業務内容に合わせて変更（users, smartphone, banknote, zap, receipt）
- タブバーを page.tsx から OcrUploadWorkbench 内に統合（タブ管理 + 設定モーダルを workbench に内包）
- page.tsx をシンプル化（OcrUploadWorkbench のみレンダリング）
- タブバーのスタイルをワークベンチのライトテーマに合わせて更新
- 要件定義.md に「§27. OCR 業務フロー詳細ルール」を追加（業務タブ大分類、命名規則、一社ルール、一括処理方針、格納先フォルダ構造）
