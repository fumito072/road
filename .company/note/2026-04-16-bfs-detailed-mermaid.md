# 2026-04-16 BFS保存先解決 詳細Mermaid

このメモは、2026-04-16 時点で採用した `案A: タブフォルダ探索 + 候補パス提示` を、ユーザーが指定した順序に合わせて整理し直した詳細版のフロー図である。

この版の主眼は以下。

- 業務タブ選択からアップロードまでを一直線の業務フローとして示す
- OCR 後に `顧客名 / 契約ID / 書類種別` を取得し、出力ファイル候補を生成する
- 顧客名の読み方を UI に出し、ユーザーに選択させる
- その後に顧客名候補を UI に出し、ここで顧客名を確定させる
- 確定した顧客名配下だけを backend が BFS 探索する
- 選択した業務タブが存在するかどうかで明確に分岐する
- 既存の業務タブがある場合は、その配下の配置候補パスをすべて UI に出してユーザーに選択させる
- 既存の業務タブがない場合は、顧客フォルダ配下に業務タブを新規作成する案を提示する
- `アップロード確認ボタン` を押した後にのみアップロードする

---

## 1. OCR業務フロー 全体

```mermaid
---
title: OCR業務フロー 詳細版（読み方選択 -> 顧客確定 -> 顧客配下BFS）
---
flowchart TD
    Start([開始]) --> TabSelect[業務タブ選択]
    TabSelect --> RuleLoad[業務タブに紐づく命名規則を読み込み]
    RuleLoad --> Upload[対象ファイルアップロード]
    Upload --> OcrRun[OCR実行]
    OcrRun --> Extract[顧客名 契約ID 書類種別を取得]
    Extract --> BuildNames[出力ファイル候補生成]
    BuildNames --> KanaUI[顧客名の読み方候補を UI に表示]
    KanaUI --> UserPickKana[ユーザーが読み方を選択]
    UserPickKana --> CustomerUI[顧客名候補を UI に表示]
    CustomerUI --> UserPickCustomer[ユーザーが顧客名を選択して確定]
    UserPickCustomer --> BFS[確定した顧客名配下を BFS 探索]
    BFS --> CheckTab{選択した業務タブが存在するか}
    CheckTab -- ある --> ExistingTab[業務タブ配下の候補パスを全件取得]
    CheckTab -- ない --> NewTabPlan[顧客フォルダ配下に業務タブ新規作成案]
    ExistingTab --> PathUI[候補パス一覧を UI に表示]
    PathUI --> UserPickPath[ユーザーが配置先パスを指定]
    NewTabPlan --> NewPathUI[新規作成パスを UI に表示]
    NewPathUI --> UserApproveNewPath[ユーザーが新規作成案を確認]
    UserPickPath --> UploadConfirm[アップロード確認ボタン]
    UserApproveNewPath --> UploadConfirm
    UploadConfirm --> UploadExec[アップロード]
    UploadExec --> Finish[完了表示]

    Finish --> End([終了])
```

---

## 2. 顧客名読み方選択と顧客確定 UI フロー

```mermaid
---
title: 顧客名読み方選択と顧客確定 UI フロー
---
flowchart TD
    A[OCR結果受信] --> B[OCR から顧客名候補文字列を抽出]
    B --> C[読み候補を生成]
    C --> D[UI に読み候補一覧を表示]
    D --> E{ユーザーがどの読み方を採用するか}
    E -- 候補選択 --> F[採用する読み方を確定]
    E -- 手修正 --> G[読み方を手入力]
    G --> F

    F --> H[確定した読み方から顧客探索ルートを絞る]
    H --> I[顧客名候補を収集]
    I --> J1[OCR 本文由来の顧客候補]
    I --> J2[ファイル名由来の顧客候補]
    I --> J3[SharePoint 顧客フォルダ由来候補]
    J1 --> K[候補を統合して UI に表示]
    J2 --> K
    J3 --> K

    K --> L[候補ごとに根拠を表示]
    L --> M[候補ごとに顧客フォルダ候補パスを表示]
    M --> N{ユーザーがどの顧客名を採用するか}
    N -- 候補選択 --> O[顧客名を確定]
    N -- 手修正 --> P[顧客名を手入力]
    P --> O

    O --> Q[確定した顧客名で顧客フォルダを再照合]
    Q --> R{顧客フォルダが見つかるか}
    R -- Yes --> S[顧客確定完了]
    R -- No --> T[新規顧客として扱う警告を表示]
    T --> U{そのまま進むか}
    U -- 再修正 --> K
    U -- 進む --> S
```

---

## 3. 顧客配下BFSと業務タブ分岐

```mermaid
---
title: 顧客配下BFSと業務タブ有無の分岐
---
flowchart TD
    A[入力: 確定した顧客名 読み方 選択業務タブ] --> B[顧客フォルダを起点にする]
    B --> C[BFS で顧客配下の全フォルダを探索]
    C --> D{選択した業務タブ名のフォルダがあるか}

    D -- ある --> E[一致した業務タブフォルダを取得]
    E --> F[その配下の既存パスをすべて列挙]
    F --> G[候補パス一覧として UI に返却]

    D -- ない --> H[顧客フォルダ配下に業務タブ新規作成案を生成]
    H --> I[新規作成後の保存先パス案を UI に返却]
```

---

## 4. 既存業務タブ配下の配置先選択フロー

```mermaid
---
title: 既存業務タブ配下の配置先選択フロー
---
flowchart TD
    A[業務タブ一致フォルダを取得] --> B[その配下のパスを全件取得]
    B --> C[フォルダごとの絶対パスを整形]
    C --> D[UI に候補パス一覧を表示]
    D --> E[候補ごとに既存フォルダであることを表示]
    E --> F{ユーザーがどこに配置するか}
    F -- 候補選択 --> G[配置先パス確定]
    F -- 候補なし --> H[別パスを手入力または再探索]
    H --> G
```

---

## 5. 新規業務タブ作成とアップロード確認

```mermaid
---
title: 新規業務タブ作成とアップロード確認
---
flowchart TD
    A[業務タブが見つからない] --> B[顧客フォルダ配下に新規業務タブ作成案を生成]
    B --> C[新規作成予定パスを UI に表示]
    C --> D[出力ファイル候補も同じ画面に表示]
    D --> E[アップロード確認ボタンを表示]
    E --> F{ユーザーが確認ボタンを押したか}
    F -- No --> G[画面上で修正を継続]
    F -- Yes --> H[必要なフォルダを作成]
    H --> I[ファイルをアップロード]
    I --> J[完了表示]

    K[既存パス選択済み] --> L[出力ファイル候補を確認画面に表示]
    L --> M[アップロード確認ボタンを表示]
    M --> N{ユーザーが確認ボタンを押したか}
    N -- No --> O[候補パスやファイル名を再修正]
    N -- Yes --> P[選択済みパスへアップロード]
    P --> Q[完了表示]
```

---

## 6. 実装メモ

- フロー順序は `業務タブ選択 -> 命名規則読込 -> ファイルアップロード -> OCR -> 顧客名/契約ID/書類種別取得 -> 出力ファイル候補生成 -> 読み方選択 -> 顧客確定 -> 顧客配下BFS -> 業務タブ分岐 -> アップロード確認 -> アップロード` とする
- 顧客名の読み方は UI で候補表示し、ユーザー選択を必須にする
- 顧客名も UI で候補表示し、その場で確定させる
- BFS の探索開始点は `確定した顧客名フォルダ` に限定する
- 業務タブが既に存在する場合は、その配下の配置候補パスをすべて UI に出す
- 業務タブが存在しない場合は、顧客フォルダ配下への新規作成案を UI に出す
- どちらの分岐でも `アップロード確認ボタン` を押すまでは保存しない